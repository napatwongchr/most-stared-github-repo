test("Repository list should render properly", () => {
  expect(1).toBe(1);
});

test("Repository list should show 'No repository found.' when no repository result", () => {
  expect(1).toBe(1);
});

test("Repository list should show 'Error fetching repositories.' when error occured", () => {
  expect(1).toBe(1);
});

test("Repository list should fetch when scroll to the end", () => {
  expect(1).toBe(1);
});

test("Repository item should contains information", () => {
  expect(1).toBe(1);
});
